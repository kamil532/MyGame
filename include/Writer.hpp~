#ifndef WRITER_HPP
#define WRITER_HPP
#include "Heders.hpp"

class Writer{
public: 
  Writer(){}
  void Write()const{};
  
private:  
  TTF_Font *font;
  
};
typedef boost::shared_ptr<Writer> WriterPtr;

#endif
